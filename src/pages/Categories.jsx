import React from 'react'
import checkAuth from '../guards/checkAuth'

function Categories() {
  return <h1 style={{backgroundColor: 'red'}}>Categories</h1>

}

// export default checkAuth(MainPage)
export default Categories
